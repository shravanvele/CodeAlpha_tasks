<li class="comment-container">
    <div class="comment-content">
    <p>
        <% if (locals.user && comment.user && locals.user.id && comment.user.id && locals.user.id.toString() === comment.user.id.toString()) { %>
            <small class="comment-actions">
                <a href="/comments/delete/<%= comment.id %>">X</a>
            </small>
        <% } %>
        

          <%= comment.content %>
          <br>
          <small>
              <%= comment.user.name %>
          </small>

          <!-- CHANGE :: display the likes of this comment, if the user is logged in, then show the link to toggle likes, else, just show the count -->
            <small>
                <% if (locals.user){ %>
                    <a class="toggle-like-button" data-likes="<%= comment.likes.length %>" href="/likes/toggle/?id=<%=comment._id%>&type=Comment">
                            <%= comment.likes.length %> Likes
                    </a>
                <% }else{ %>
                    <%= comment.likes.length %> Likes
                <% } %>
            </small>
      </p> 
    </div>   
</li>